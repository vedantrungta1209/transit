# Root compose variables — copy to .env and fill in
# These are used by docker-compose.prod.yml only (never sent to app code)

DB_USER=transit
DB_PASSWORD=              # openssl rand -hex 16
REDIS_PASSWORD=           # openssl rand -hex 16
